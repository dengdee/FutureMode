# Copy the frontend value to frontend/.env.local when it changes.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Backend settings. The backend reads this root file when copied to .env.
APP_ENV=development
API_HOST=127.0.0.1
API_PORT=8000
CORS_ORIGINS=http://localhost:3000

